<div class="container py-5">
  <h2>My lives</h2>

  <div class="cards">
    <% @lives.each do |life| %>
      <div class="card p-3">
          <h3 class="card-title"><%= link_to life.title, life_path(life) %></h3>
          <p class="card-text"><%= life.description %></p>
          <p class="card-text"><%= life.price %>$</p>
      </div>
    <% end %>
  </div>
</div>
